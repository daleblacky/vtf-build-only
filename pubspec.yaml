name: vexlorgrid_command_deck
description: Internal-only mock-data Flutter prototype for VexlorGrid Command Deck V1.
publish_to: 'none'
version: 0.1.1+2

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  path_provider: ^2.1.5
  shared_preferences: ^2.3.3
  crypto: ^3.0.6
  workmanager: ^0.8.0

flutter:
  uses-material-design: true
